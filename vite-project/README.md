Created a backend server using Express and added JWT for login authentication.
Built a login API that checks username and password and returns a token.
Stored the token in localStorage after successful login in React.
Created protected routes in React using React Router to allow access only if logged in.
Verified the token on the backend using middleware before allowing access to protected data.